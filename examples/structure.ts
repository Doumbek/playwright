import { test } from "@playwright/test"

test.describe("Home page", () => {
    test.beforeAll("Before all", async ({ page }) => {

    })

    test.beforeEach("Before each", ({ page }) => {

    })

    test("should have 'Sign in' button visible", async ({ page }) => {

    })

    test("should have 9 items in product list", async ({ page }) => {
        // const products = 
    })

    test("should have item with title in product list", async ({ page }) => {
        // const products = 
    })

    test.afterEach("After each", ({ page }) => {

    })

    test.afterAll("After all", ({ page }) => {

    })
})


